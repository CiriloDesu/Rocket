// Estrutura de repetição
// for(inicialização de uma variável; condição de continuação para o loop; expressão final)
// continua - pula execuçãodo momento


for(let i = 0; i < 10; i++) {
    console.log(`o número atual é ${i}`)
}